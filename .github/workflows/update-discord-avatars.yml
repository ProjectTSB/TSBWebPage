name: Update Discord avatars

on:
  schedule:
    # 毎日 00:00 UTC に実行（日本時間 09:00）
    - cron: "0 0 * * *"
  # 手動実行用
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-discord-avatars:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Update creator avatars
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          DRY_RUN: ${{ vars.DRY_RUN }}
        run: node scripts/update-discord-avatars.mjs

      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: Discordアバターを更新
          branch: bot/update-discord-avatars
          delete-branch: true
          title: "🤖 Discordアバターを自動更新"
          body: |
            Discord API から最新のアバター画像を取得して更新しました。

            - 自動生成されたPRです
            - 必須チェックが通過したら自動的にマージされます
          labels: bot

      - name: Enable auto-merge
        if: steps.cpr.outputs.pull-request-number
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr merge --auto --merge ${{ steps.cpr.outputs.pull-request-number }}
